# Allie Swezey Consulting — Preview Site

Static snapshot of the current marketing site for review. No build tooling; just raw HTML/CSS.

## Pages
- `/` — main site (`index.html`)
- `/sample.html` — gated sample preview + form
- `/contact.html` — strategy call request form
- `/allieswezey.html` — legacy layout (kept for comparison)

## Preview Hosting Plan
1. Push this folder to GitHub (`mattswezey/allie-consulting-preview`).
2. Enable GitHub Pages (main branch, root) to generate a quick shareable link.
3. Add `robots.txt` + `noindex` meta tags to avoid accidental indexing.
4. Once Allie approves, migrate the site into the real repo + Netlify deployment.
